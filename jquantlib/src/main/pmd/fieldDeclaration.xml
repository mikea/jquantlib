<?xml version="1.0"?>
<ruleset name="Field type rules">
	<description>Checks field type rules</description>
	<rule name="ListPrivateNonFinalFields" message="Non-final method parameter"
		class="net.sourceforge.pmd.rules.XPathRule">
		<description>Detect uses of non final method parameter.
		</description>
		<properties>
			<property name="xpath">
				<value>
				<![CDATA[
          			//FieldDeclaration[@Private='true' and @Final='false']
                ]]>
                </value>
			</property>
		</properties>
		<example>
		<![CDATA[
//
// Private fields in general are initialized by constructors.
// So they should be made final.
//
// For a good reason, a field can be left non-final, because it is probably
// used as a variable intende to keep state.
//
// This verification helps developers to review their code and find objects
// left not initialized by constructors.
//

private final Observable observable;
private counter;
	 
public MyClass(Class parent) {
    observable = Observable.class.isAssignableFrom(parent) ? parent : null;
    counter = 0;
} 

public run() {
    // ... do something here... 
    count += 1;
}
        ]]></example>
	</rule>
</ruleset>
