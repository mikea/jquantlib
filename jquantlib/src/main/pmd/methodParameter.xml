<?xml version="1.0"?>
<ruleset name="Method parameter rules">
	<description>Checks method parameter rules</description>
	<rule name="ListNonFinalMethodParameter" message="Non-final method parameter"
		class="net.sourceforge.pmd.rules.XPathRule">
		<description>Detect uses of non final method parameter.
		</description>
		<properties>
			<property name="xpath">
				<value>
				<![CDATA[
		            //FormalParameter[@Final='false']
                ]]></value>
			</property>
		</properties>
		<example>
		<![CDATA[
//
// Received parameter values should not be changed inside the method, otherwise
// the received passed parameter is lost and/or cannot be used safely
// inside the method.
//
void method(String arg) { ... } // Don't do this
void method(final String arg) { ... } // Do this instead
        ]]>
        </example>
	</rule>
</ruleset>
