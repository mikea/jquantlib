<?xml version="1.0"?>
<ruleset name="Vector rules">
	<description>Checks vector rules</description>
	<rule name="Vectors" message="Don't use java.util.Vector"
		class="net.sourceforge.pmd.rules.XPathRule">
		<description>Detect uses of Vector</description>
		<properties>
			<property name="xpath">
				<value>
				<![CDATA[
          			//ClassOrInterfaceType[@Image='Vector' or @Image='java.util.Vector']
      			]]>
				</value>
			</property>
		</properties>
		<example>
<![CDATA[
   //
   // Do not use Vector, it can't be un-synchronized
   //
   Vector<String>> vector=new Vector<String>(); // Don't do this
   List<String> list=new ObjectArrayList<String>(); // Do this instead
   ]]>
		</example>
	</rule>
</ruleset>
